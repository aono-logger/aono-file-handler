{
  "name": "aono-file-handler",
  "version": "0.4.1",
  "description": "file handler with formatting and rotation support for aono logger",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf lib #",
    "compile": "tsc",
    "test": "TS_NODE_FILES=1 mocha --use_strict -r source-map-support/register -r coffeescript/register -r ts-node/register src/mocha.config.coffee $(find src -name \"*.spec.coffee\")",
    "copy": "mkdir -p lib/ && cp package.json lib/",
    "build": "npm run compile && npm run copy && npm run test",
    "clean-build": "npm run clean && npm run build",
    "watch": "watch 'npm run clean-build' src",
    "deploy": "npm install && npm run clean-build && npm publish lib"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aono-logger/aono-file-handler.git"
  },
  "keywords": [
    "file",
    "handler",
    "aono",
    "node",
    "logger",
    "log",
    "logging",
    "typescript"
  ],
  "author": "Maciej Chałapuk <maciej@chalapuk.pl>",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/aono-logger/aono-file-handler/issues"
  },
  "homepage": "https://github.com/aono-logger/aono-file-handler#readme",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^12.7.2",
    "aono": "^0.6.0",
    "coffeescript": "^2.3.2",
    "fake-promise": "^2.3.2",
    "glob": "^7.1.3",
    "mocha": "^5.2.0",
    "should": "^13.2.3",
    "should-sinon": "0.0.6",
    "sinon": "^4.5.0",
    "source-map-support": "^0.5.9",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  }
}
